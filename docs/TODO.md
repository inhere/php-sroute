# todo

- 调整参数路由的解析，由现在的正则分析 -> 使用字符串分析(**无提升**)
- 增加属性 `$routesData` 存储路由中不用于匹配的数据，减轻现有路由数据的复杂度
  - 现有的变量 `$routesData` 改成 `$routesInfo`

## modified

 - 抽离静态路由的信息
 - 独立一个扁平化的静态路由结构
 - 有规律的动态路由匹配调整
 - 分离常驻内存应用才使用到的 动态路由缓存
